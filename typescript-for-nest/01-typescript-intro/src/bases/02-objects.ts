export const ids: number[] = [11,22,33];

interface user {
    name: String,
    age?: number,
}
const user: user = {
    name: 'blbl',
    age: 5
}

export const usuarios: user[] = [];

usuarios.push(user);